import { faker } from "@faker-js/faker";

//importar entities aqui
import jwt from "jsonwebtoken";

const generateCompany = () => {};

const generateAdvert = () => {};

const generateToken = () => {};

const generateAdministrator = () => {};

const generateMessage = () => {};

export {
  generateCompany,
  generateAdvert,
  generateAdministrator,
  generateToken,
  generateMessage,
};
